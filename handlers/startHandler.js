export const startHandler = (userId) => {

    console.log(`Start handler called by userId: ${userId}`);

    const reply = 'Welcome to the all-in-one Solana bot. Please tell me what to do.';

    return reply;
};